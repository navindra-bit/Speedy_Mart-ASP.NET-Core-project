@*     @{
        if(ViewData.ContainsKey("Brand"))
        {

        }
         @TempData["ptitle"] 
        } *@


@{
    TempData.Keep("ptitle");
} 


<div id="myCarousel" class="carousel slide" data-bs-ride="carousel">
    <!-- Indicators -->
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#myCarousel" data-bs-slide-to="0" class="active " aria-current="true" aria-label="Slide 1" style="background-color:red"></button>
        <button type="button" data-bs-target="#myCarousel" data-bs-slide-to="1" aria-label="Slide 2" style="background-color:red"></button>
        <button type="button" data-bs-target="#myCarousel" data-bs-slide-to="2" aria-label="Slide 3" style="background-color:red"></button>
    </div>

    <!-- Slides -->
    <div  class="carousel-inner">
        <div class="carousel-item active">
            <img style="height:300px"  src="~/Image/brandedfoods .jpg" class="d-block w-100" alt="Image 1">
        </div>
        <div class="carousel-item">
            <img style="height:300px" src="~/Image/haircare.jpg" class="d-block w-100" alt="Image 2">
        </div>
        <div class="carousel-item">
            <img style="height:300px" src="~/Image/skin-care.jpg" class="d-block w-100" alt="Image 3">
        </div>
    </div>

    <!-- Controls -->
    <button class=" carousel-control-prev" type="button" data-bs-target="#myCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true" ></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#myCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>
 
 
<div>

    
    <div class="container my-4">
        <h5 class="mb-3">@ViewData["Ptitle"]?.ToString()</h5>

   
        <div class="d-flex overflow-auto px-2 gap-3">

            @foreach (var item in ViewBag.Brand)
            {
                <a >
                    <div class="card text-center flex-shrink-0" style="width: 165px;">
                        
                        <img src="@item.Link"  class="card-img-top p-2" style="height: 120px; object-fit: contain;cursor:pointer" alt="@item">
                        <div class="card-body p-2">

                            <p class="card-text mb-1" style="font-size: 14px;">@item.Name</p>

                            <strong style="font-size: 13px; color: green;">@item.Offer</strong>
                        </div>
                    </div>
                </a>
            }
        </div>
    </div>







        <div>

        <h5 style="margin: 20px 0 10px 15px;">@TempData.Peek("ptitle")</h5>

        </div>
</div>
